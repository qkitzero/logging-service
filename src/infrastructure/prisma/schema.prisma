generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

model Log {
  id          String   @id
  serviceName String
  level       String
  message     String
  timestamp   DateTime
  userId      String?

  @@map("logs")
}
